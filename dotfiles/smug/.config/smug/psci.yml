session: psci

root: ~/code/psci

windows:
  - name: code
    root: chatbot-ui
    selected: true
    commands:
      - nvim .

  - name: terminals
    root: chatbot-ui
    layout: main-horizontal
    panes:
      - type: horizontal
        root: shared-services-api
      - type: horizontal
        root: labs/agenting

  - name: psci-hq
    root: /Users/luke/code/psci-hq
    commands:
      - pnpm dev
